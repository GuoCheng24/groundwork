"""groundwork — the cheap work that decides whether the expensive work is worth doing.

A research pipeline for coding agents: direction, gate, experiment, claim,
paper, submission, memory. The stage the others are missing is the first one,
which returns NO-GO.
"""
__version__ = "0.1.0"
